/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cssshowpsbdvitncryqv.supabase.co'], // 허용된 도메인 추가
  },
};

export default nextConfig;
